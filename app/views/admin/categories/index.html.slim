.card
  .card-body
    = react_component "admin/CreateCategory", { categories: @categories.main }

    hr

    - if @categories.any?
      table.table.table-striped
        thead
          tr
            th Nombre
            th Padre
            th Propiedades
            th Acciones
        tbody
          = render @categories
    - else
      h6 Aún no has creado ninguna categoria
